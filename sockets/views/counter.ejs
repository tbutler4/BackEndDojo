<!DOCTYPE html>
<html>
<head>
	<title>Epic Button</title>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
    <script type="text/javascript" src="/socket.io/socket.io.js"></script>
    <script type ="text/javascript">
		$(document).ready(function (){
	            var socket = io.connect(); //1
	            $('#btn').click(function (){
	                socket.emit("button_click") //3
	            });
	            $('#reset').click(function (){
	                socket.emit("reset") //7
	            });
	            socket.on('update_count', function(data){ //6 & 10
	                $("#count").html(data.count);
	           	});
	        });
	  </script>
</head>
<body>
<h1>The button has been pushed <span id="count">0</span> time(s)</h1>
	<p>Push the button to update the count!</p>
	<button id="btn" >PUSH THE EPIC BUTTON</button><br>
	<button id="reset">Reset count</button>
</body>
</html>